# Generalist Persona (Plan 3 — not yet implemented)

This persona is reserved for users who fit neither developer nor lawyer. Plan 3 will populate it with: minimal universal skills (`/start-session`, `/save-session`, `/save-lightweight`, `/capture`, `/vault-search`, `/brain-check`, `/create-project`), generic vocabulary, no external folder convention, and a minimal `_HowThisWorks.md`.

If you pick this persona during setup right now, _setup.sh will fail with a clear "not yet implemented" message.
